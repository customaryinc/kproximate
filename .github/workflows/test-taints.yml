name: Test Taint Implementation

on:
  push:
    paths:
      - 'kproximate/scaler/**'
      - 'kproximate/kubernetes/**'
      - 'kproximate/config/**'
  pull_request:
    paths:
      - 'kproximate/scaler/**'
      - 'kproximate/kubernetes/**'
      - 'kproximate/config/**'

jobs:
  test-taints:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.23'
    
    - name: Test taint parsing
      run: |
        cd kproximate
        go mod tidy
        go test -v ./scaler -run TestRequiredScaleEventsFor1CPU
    
    - name: Set up Helm
      uses: azure/setup-helm@v3
      with:
        version: '3.12.0'
    
    - name: Update Helm dependencies
      run: helm dependency update chart/kproximate
    
    - name: Validate example configuration
      run: |
        helm template test chart/kproximate --values examples/example-values-with-taints.yaml > /tmp/rendered.yaml
        grep -q "kpNodeTaints" /tmp/rendered.yaml && echo "✓ Taint configuration found in rendered template"
